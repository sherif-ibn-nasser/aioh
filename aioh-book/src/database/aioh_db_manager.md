## AiohDBManager

This class serves as the primary interface for database operations. It provides a comprehensive Java API to perform the
following actions:

* Create new databases.
* Read and query existing databases.
* Delete databases.
* Establish connections to databases stored locally on the machine.

src/main/java/com/aioh/database/AiohDBManager.java:

```java
{{#include ../../../src/main/java/com/aioh/database/AiohDBManager.java}}
```